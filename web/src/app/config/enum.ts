export enum AUTH_TYPE {
    LOGIN = 'login',
    REGISTER = 'register'
}

export enum TYPES {
    USER = 'user',
    TAG = 'tag',
    CATEGORY = 'category',
    TRANSACTION = 'transaction',
    ITEM = 'item'
}